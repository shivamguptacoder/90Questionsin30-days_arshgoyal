	void matchPairs(char nuts[], char bolts[], int n) {
	    // code here
	     set<char> s;
   for(int i=0;i<n;i++)
        s.insert(nuts[i]);
        
   int i = 0;
  
   for(auto item : s)
   {
        nuts[i] = item;
           bolts[i] = item;
           i++;
   }
	}
